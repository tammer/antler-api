name: Daily Backup

on:
  schedule:
    - cron: "40 07 * * *"
  workflow_dispatch:

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger backup
        run: |
          curl https://api.tammer.com/download_db
